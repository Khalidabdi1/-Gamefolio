import { useMemo, useState } from 'react';
import { ScrollView, Text, TextInput, View, Pressable, useWindowDimensions } from 'react-native';
import { useSafeAreaInsets } from 'react-native-safe-area-context';
import { games } from '@/data/games';
import { GameCard } from '@/components/game-card';
import { useLibrary } from '@/state/library';
import { Icon } from '@/ui/icon';
export default function Search(){const [query,setQuery]=useState('');const [genre,setGenre]=useState('All');const {width}=useWindowDimensions();const insets=useSafeAreaInsets();const s=useLibrary();const found=useMemo(()=>games.filter(g=>(genre==='All'||g.genre===genre)&&`${g.title} ${g.genre} ${g.platforms}`.toLowerCase().includes(query.toLowerCase().trim())),[query,genre]);return <ScrollView className="flex-1 bg-canvas" keyboardShouldPersistTaps="handled" contentContainerStyle={{paddingTop:Math.max(insets.top,48)+20,paddingBottom:140}}><Text className="px-5 text-ink text-3xl font-bold">Find your next adventure</Text><View className="mx-5 mb-5 mt-6 flex-row items-center gap-3 rounded-2xl bg-surface px-4"><Icon name="search" color="#928c8e" size={21}/><TextInput accessibilityLabel="Search games" placeholder="Games, genres, platforms" placeholderTextColor="#928c8e" value={query} onChangeText={setQuery} autoCorrect={false} returnKeyType="search" style={{flex:1,color:'#f5f4f0',fontSize:16,paddingVertical:15}}/>{query&&<Pressable accessibilityLabel="Clear search" onPress={()=>setQuery('')}><Icon name="close" size={18}/></Pressable>}</View><ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={{paddingHorizontal:20,gap:8,paddingBottom:28}}>{['All',...new Set(games.map(g=>g.genre))].map(item=><Pressable key={item} onPress={()=>setGenre(item)} accessibilityState={{selected:genre===item}} className="rounded-full px-4 py-2" style={{backgroundColor:genre===item?'#ff624f':'#242122'}}><Text style={{color:genre===item?'#160c0a':'#eee8e8',fontWeight:'500'}}>{item}</Text></Pressable>)}</ScrollView><Text className="mb-4 px-5 text-ink text-2xl font-semibold">{query?'Search results':'Discover games'}</Text><View className="flex-row flex-wrap px-5" style={{gap:12,rowGap:26}}>{found.map(g=><GameCard key={g.id} game={g} entry={s.entries[g.id]} width={(width-64)/3}/>)}</View>{!found.length&&<View className="px-5 py-12"><Text className="text-center text-xl text-ink">No games found</Text><Text className="mt-3 text-center text-muted">Try another title, platform or genre.</Text></View>}<Text className="mt-9 px-5 text-center text-xs text-muted">A curated starter catalog · Library saved on this device</Text></ScrollView>;}
